################## GIT ALIASES #######################

# forgit aliases defined in $DOTDIR/git/forgit.zsh
#glo - interactive git log with fzf preview and bat
#ga - interactive git add
#gd - interactive git diff


#from git-extra-commands
alias gitroot='cd $(git rev-parse --show-toplevel) && echo "$_"'
alias git-cdroot='cd $(git rev-parse --show-toplevel) && echo "$_"'

#More aliases
alias gs='git status'
